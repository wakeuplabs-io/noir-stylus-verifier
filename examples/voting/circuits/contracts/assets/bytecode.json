{"noir_version":"1.0.0-beta.6+e796dfd67726cbc28eb9991782533b211025928d","hash":"336008086243406863","abi":{"parameters":[{"name":"root","type":{"kind":"field"},"visibility":"public"},{"name":"path","type":{"kind":"array","length":2,"type":{"kind":"field"}},"visibility":"private"},{"name":"direction_selector","type":{"kind":"array","length":2,"type":{"kind":"boolean"}},"visibility":"private"},{"name":"secret","type":{"kind":"field"},"visibility":"private"},{"name":"priv_key","type":{"kind":"field"},"visibility":"private"},{"name":"nullifier","type":{"kind":"field"},"visibility":"public"},{"name":"proposal_id","type":{"kind":"field"},"visibility":"public"},{"name":"vote","type":{"kind":"integer","sign":"unsigned","width":8},"visibility":"public"}],"return_type":null,"error_types":{"176791207579795066":{"error_kind":"string","string":"Commitment not in Merkle Tree"},"11434998451959231239":{"error_kind":"string","string":"Invalid vote"},"17824934553683747113":{"error_kind":"string","string":"Invalid nullifier"}}},"bytecode":"","debug_symbols":"","file_map":{"50":{"source":"use poseidon::poseidon2::Poseidon2::hash;\n\npub fn commitment(private_key: Field, secret: Field) -> Field {\n    hash([private_key, secret], 2)\n}\n\npub fn nullifier(root: Field, priv_key: Field, proposal_id: Field) -> Field {\n    hash([root, priv_key, proposal_id], 3)\n}\n\n","path":"/Users/matzapata/git-work/aztec/noir-stylus-verifier/examples/voting/circuits/src/definitions.nr"},"51":{"source":"mod definitions;\nmod merkle_tree;\n\nuse poseidon::poseidon2::Poseidon2::hash;\n\nglobal DEPTH: u32 = 2;\n\nfn main(\n    root: pub Field,\n    path: [Field; DEPTH],\n    direction_selector: [bool; DEPTH],\n    secret: Field,\n    priv_key: Field,\n    nullifier: pub Field,\n    proposal_id: pub Field,\n    vote: pub u8,\n) {\n    let note_commitment = definitions::commitment(priv_key, secret);\n\n    merkle_tree::assert_merkle_inclusion(\n        root,\n        path,\n        direction_selector,\n        note_commitment,\n        \"Commitment not in Merkle Tree\",\n    );\n    assert(nullifier == definitions::nullifier(root, priv_key, proposal_id), \"Invalid nullifier\");\n    assert(vote <= 1, \"Invalid vote\");\n}\n\n#[test]\nfn test_valid_vote_yes() {\n    // Test data setup\n    let priv_key = 123456789;\n    let secret = 987654321;\n    let proposal_id = 1;\n    let vote = 1; // YES vote\n    \n    // Create commitment\n    let note_commitment = definitions::commitment(priv_key, secret);\n    \n    // Build a simple merkle tree with depth 2\n    // Tree structure:\n    //       root\n    //      /    \\\n    //   left    right\n    //   /  \\    /   \\\n    // com   0  0     0\n    let sibling = definitions::commitment(0, 0); // dummy commitment\n    let left_branch = hash([note_commitment, sibling], 2);\n    let right_branch = hash([sibling, sibling], 2);\n    let root = hash([left_branch, right_branch], 2);\n    \n    // Path and direction for leftmost leaf\n    let path = [sibling, right_branch];\n    let direction_selector = [false, false]; // left, left\n    \n    let nullifier = definitions::nullifier(root, priv_key, proposal_id);\n    \n    // This should pass\n    main(root, path, direction_selector, secret, priv_key, nullifier, proposal_id, vote);\n\n    // print root, path, direction_selector, secret, priv_key, nullifier, proposal_id, vote\n    std::println(root);\n    std::println(path);\n    std::println(direction_selector);\n    std::println(secret);\n    std::println(priv_key);\n    std::println(nullifier);\n    std::println(proposal_id);\n    std::println(vote);\n}\n\n#[test]\nfn test_valid_vote_no() {\n    // Test data setup\n    let priv_key = 555666777;\n    let secret = 111222333;\n    let proposal_id = 2;\n    let vote = 0; // NO vote\n    \n    let note_commitment = definitions::commitment(priv_key, secret);\n    \n    // Build merkle tree - place commitment at index 1 (right leaf of left branch)\n    let sibling = definitions::commitment(0, 0);\n    let left_branch = hash([sibling, note_commitment], 2);\n    let right_branch = hash([sibling, sibling], 2);\n    let root = hash([left_branch, right_branch], 2);\n    \n    let path = [sibling, right_branch];\n    let direction_selector = [true, false]; // right, left\n    \n    let nullifier = definitions::nullifier(root, priv_key, proposal_id);\n    \n    main(root, path, direction_selector, secret, priv_key, nullifier, proposal_id, vote);\n}\n\n#[test(should_fail_with = \"Invalid vote\")]\nfn test_invalid_vote_value() {\n    let priv_key = 123456789;\n    let secret = 987654321;\n    let proposal_id = 1;\n    let vote = 2; // Invalid vote (must be 0 or 1)\n    \n    let note_commitment = definitions::commitment(priv_key, secret);\n    let sibling = definitions::commitment(0, 0);\n    let left_branch = hash([note_commitment, sibling], 2);\n    let right_branch = hash([sibling, sibling], 2);\n    let root = hash([left_branch, right_branch], 2);\n    \n    let path = [sibling, right_branch];\n    let direction_selector = [false, false];\n    let nullifier = definitions::nullifier(root, priv_key, proposal_id);\n    \n    main(root, path, direction_selector, secret, priv_key, nullifier, proposal_id, vote);\n}\n\n#[test(should_fail_with = \"Invalid nullifier\")]\nfn test_invalid_nullifier() {\n    let priv_key = 123456789;\n    let secret = 987654321;\n    let proposal_id = 1;\n    let vote = 1;\n    \n    let note_commitment = definitions::commitment(priv_key, secret);\n    let sibling = definitions::commitment(0, 0);\n    let left_branch = hash([note_commitment, sibling], 2);\n    let right_branch = hash([sibling, sibling], 2);\n    let root = hash([left_branch, right_branch], 2);\n    \n    let path = [sibling, right_branch];\n    let direction_selector = [false, false];\n    \n    // Wrong nullifier - using different proposal_id\n    let wrong_nullifier = definitions::nullifier(root, priv_key, proposal_id + 1);\n    \n    main(root, path, direction_selector, secret, priv_key, wrong_nullifier, proposal_id, vote);\n}\n\n#[test(should_fail_with = \"Commitment not in Merkle Tree\")]\nfn test_invalid_merkle_proof() {\n    let priv_key = 123456789;\n    let secret = 987654321;\n    let proposal_id = 1;\n    let vote = 1;\n    \n    let note_commitment = definitions::commitment(priv_key, secret);\n    let sibling = definitions::commitment(0, 0);\n    \n    // Create a tree where the commitment is NOT included\n    let other_commitment = definitions::commitment(999, 888);\n    let left_branch = hash([other_commitment, sibling], 2);\n    let right_branch = hash([sibling, sibling], 2);\n    let root = hash([left_branch, right_branch], 2);\n    \n    // But provide path as if our commitment was included\n    let path = [sibling, right_branch];\n    let direction_selector = [false, false];\n    let nullifier = definitions::nullifier(root, priv_key, proposal_id);\n    \n    main(root, path, direction_selector, secret, priv_key, nullifier, proposal_id, vote);\n}\n\n#[test(should_fail_with = \"Invalid nullifier\")]\nfn test_nullifier_with_wrong_private_key() {\n    let priv_key = 123456789;\n    let secret = 987654321;\n    let proposal_id = 1;\n    let vote = 1;\n    \n    let note_commitment = definitions::commitment(priv_key, secret);\n    let sibling = definitions::commitment(0, 0);\n    let left_branch = hash([note_commitment, sibling], 2);\n    let right_branch = hash([sibling, sibling], 2);\n    let root = hash([left_branch, right_branch], 2);\n    \n    let path = [sibling, right_branch];\n    let direction_selector = [false, false];\n    \n    // Generate nullifier with wrong private key\n    let wrong_nullifier = definitions::nullifier(root, priv_key + 1, proposal_id);\n    \n    main(root, path, direction_selector, secret, priv_key, wrong_nullifier, proposal_id, vote);\n}\n\n#[test]\nfn test_different_tree_positions() {\n    // Test commitment at different positions in the tree\n    let priv_key = 777888999;\n    let secret = 444555666;\n    let proposal_id = 3;\n    let vote = 0;\n    \n    let note_commitment = definitions::commitment(priv_key, secret);\n    let sibling = definitions::commitment(0, 0);\n    \n    // Place commitment at index 2 (left leaf of right branch)\n    let left_branch = hash([sibling, sibling], 2);\n    let right_branch = hash([note_commitment, sibling], 2);\n    let root = hash([left_branch, right_branch], 2);\n    \n    let path = [sibling, left_branch];\n    let direction_selector = [false, true]; // left, right\n    let nullifier = definitions::nullifier(root, priv_key, proposal_id);\n    \n    main(root, path, direction_selector, secret, priv_key, nullifier, proposal_id, vote);\n}\n","path":"/Users/matzapata/git-work/aztec/noir-stylus-verifier/examples/voting/circuits/src/main.nr"},"52":{"source":"use poseidon::poseidon2::Poseidon2::hash;\n\npub fn assert_merkle_inclusion<let N: u32>(\n    root: Field,\n    path: [Field],\n    direction_selector: [bool],\n    leaf: Field,\n    failure_message: str<N>,\n) {\n    assert(path.len() == direction_selector.len());\n\n    let mut digest = leaf;\n    for i in 0..path.len() {\n        let (left, right) = if direction_selector[i] {\n            (path[i], digest)\n        } else {\n            (digest, path[i])\n        };\n\n        digest = hash([left, right], 2);\n    }\n\n    assert(digest == root, failure_message);\n}\n\npub fn find_leaf_index(direction_selector: [bool]) -> Field {\n    let mut index: u32 = 0;\n    let depth = direction_selector.len();\n\n    for i in 0..depth {\n        let pos = depth - 1 - i;\n        index = index * 2;\n        if direction_selector[pos] {\n            index = index + 1;\n        }\n    }\n    index as Field\n}\n\n// assert merkle inclusion tests\n\n#[test]\nfn should_be_included() {\n    let first_level_l = hash([hash([1], 1), hash([0], 1)], 2);\n    let first_level_r = hash([hash([0], 1), hash([0], 1)], 2);\n    let root = hash([first_level_l, first_level_r], 2);\n\n    let path: [Field; 2] = [hash([0], 1), first_level_r];\n    let direction_selector: [bool; 2] = [false, false];\n\n    assert_merkle_inclusion(root, path, direction_selector, hash([1], 1), \"Failed\");\n}\n\n#[test(should_fail_with = \"Failed\")]\nfn should_not_included() {\n    let first_level_l = hash([hash([1], 1), hash([0], 1)], 2);\n    let first_level_r = hash([hash([0], 1), hash([0], 1)], 2);\n    let root = hash([first_level_l, first_level_r], 2);\n\n    let path: [Field; 2] = [hash([0], 1), first_level_r];\n    let direction_selector: [bool; 2] = [false, false];\n\n    assert_merkle_inclusion(root, path, direction_selector, hash([100], 1), \"Failed\");\n}\n\n// generate index tests\n\n#[test]\nfn test_generate_index_case1() {\n    let directions: [bool; 2] = [true, false];\n    let index = find_leaf_index(directions);\n    assert(index == 1);\n}\n\n#[test]\nfn test_generate_index_case2() {\n    let directions: [bool; 2] = [false, true];\n    let index = find_leaf_index(directions);\n    assert(index == 2);\n}\n","path":"/Users/matzapata/git-work/aztec/noir-stylus-verifier/examples/voting/circuits/src/merkle_tree.nr"},"59":{"source":"use std::default::Default;\nuse std::hash::Hasher;\n\ncomptime global RATE: u32 = 3;\n\npub struct Poseidon2 {\n    cache: [Field; 3],\n    state: [Field; 4],\n    cache_size: u32,\n    squeeze_mode: bool, // 0 => absorb, 1 => squeeze\n}\n\nimpl Poseidon2 {\n    #[no_predicates]\n    pub fn hash<let N: u32>(input: [Field; N], message_size: u32) -> Field {\n        Poseidon2::hash_internal(input, message_size, message_size != N)\n    }\n\n    pub(crate) fn new(iv: Field) -> Poseidon2 {\n        let mut result =\n            Poseidon2 { cache: [0; 3], state: [0; 4], cache_size: 0, squeeze_mode: false };\n        result.state[RATE] = iv;\n        result\n    }\n\n    fn perform_duplex(&mut self) {\n        // add the cache into sponge state\n        for i in 0..RATE {\n            // We effectively zero-pad the cache by only adding to the state\n            // cache that is less than the specified `cache_size`\n            if i < self.cache_size {\n                self.state[i] += self.cache[i];\n            }\n        }\n        self.state = crate::poseidon2_permutation(self.state, 4);\n    }\n\n    fn absorb(&mut self, input: Field) {\n        assert(!self.squeeze_mode);\n        if self.cache_size == RATE {\n            // If we're absorbing, and the cache is full, apply the sponge permutation to compress the cache\n            self.perform_duplex();\n            self.cache[0] = input;\n            self.cache_size = 1;\n        } else {\n            // If we're absorbing, and the cache is not full, add the input into the cache\n            self.cache[self.cache_size] = input;\n            self.cache_size += 1;\n        }\n    }\n\n    fn squeeze(&mut self) -> Field {\n        assert(!self.squeeze_mode);\n        // If we're in absorb mode, apply sponge permutation to compress the cache.\n        self.perform_duplex();\n        self.squeeze_mode = true;\n\n        // Pop one item off the top of the permutation and return it.\n        self.state[0]\n    }\n\n    fn hash_internal<let N: u32>(\n        input: [Field; N],\n        in_len: u32,\n        is_variable_length: bool,\n    ) -> Field {\n        let two_pow_64 = 18446744073709551616;\n        let iv: Field = (in_len as Field) * two_pow_64;\n        let mut sponge = Poseidon2::new(iv);\n        for i in 0..input.len() {\n            if i < in_len {\n                sponge.absorb(input[i]);\n            }\n        }\n\n        // In the case where the hash preimage is variable-length, we append `1` to the end of the input, to distinguish\n        // from fixed-length hashes. (the combination of this additional field element + the hash IV ensures\n        // fixed-length and variable-length hashes do not collide)\n        if is_variable_length {\n            sponge.absorb(1);\n        }\n        sponge.squeeze()\n    }\n}\n\npub struct Poseidon2Hasher {\n    _state: [Field],\n}\n\nimpl Hasher for Poseidon2Hasher {\n    fn finish(self) -> Field {\n        let iv: Field = (self._state.len() as Field) * 18446744073709551616; // iv = (self._state.len() << 64)\n        let mut sponge = Poseidon2::new(iv);\n        for i in 0..self._state.len() {\n            sponge.absorb(self._state[i]);\n        }\n        sponge.squeeze()\n    }\n\n    fn write(&mut self, input: Field) {\n        self._state = self._state.push_back(input);\n    }\n}\n\nimpl Default for Poseidon2Hasher {\n    fn default() -> Self {\n        Poseidon2Hasher { _state: &[] }\n    }\n}\n","path":"/Users/matzapata/nargo/github.com/noir-lang/poseidon/v0.1.1/src/poseidon2.nr"}},"names":["main"],"brillig_names":["directive_integer_quotient"]}